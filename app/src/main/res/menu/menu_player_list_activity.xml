<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".activities.PlayerActivity">

    <item
        android:checkable="false"
        android:enabled="false"
        android:icon="@drawable/transparent"
        android:orderInCategory="1"
        android:visible="true"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_edit_player"
        android:icon="@drawable/icon_edit_toolbar"
        android:orderInCategory="2"
        android:title="@string/action_edit_deck"
        android:visible="false"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_delete_player"
        android:icon="@drawable/icon_delete_toolbar"
        android:orderInCategory="3"
        android:title="@string/action_delete_deck"
        android:visible="false"
        app:showAsAction="always"/>
</menu>
