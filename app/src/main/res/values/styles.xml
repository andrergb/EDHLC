<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Base.Theme.AppCompat.Light.DarkActionBar">
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@android:color/transparent</item>
        <item name="background">@android:color/transparent</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="actionMenuTextColor">@drawable/white_checked</item>
        <item name="android:actionMenuTextColor">@drawable/white_checked</item>
    </style>

    <style name="MyPopupBar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:colorBackground">@color/gray200</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <!-- ActionBar for Deck activity-->
    <style name="MyActionBarDeck" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/primary_color</item>
        <item name="background">@color/primary_color</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <!-- ActionBar Parallax styles -->
    <style name="MyActionBarParallax" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="CollapsedAppBarExpanded" parent="@android:style/Widget.Material.Toolbar">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">"sans-serif-medium"</item>
    </style>

    <style name="CollapsedAppBarCollapsed" parent="@android:style/Widget.Material.Toolbar">
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">"sans-serif-medium"</item>
    </style>
</resources>